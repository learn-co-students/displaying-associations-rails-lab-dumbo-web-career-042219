<h1>Artist:</h1>
Name: <%= @artist.name %>
 <%= @artist.song_count %> songs
<h3>List of Songs:</h3>
<ul>
<% @artist.songs.each do |song| %>

    <li><%= link_to song.title, song_path(song) %></li>

  <% end %>
  </ul>
